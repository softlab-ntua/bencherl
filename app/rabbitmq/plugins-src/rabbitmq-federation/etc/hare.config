%% -*- erlang -*-
[{rabbitmq_federation,
  [{upstream_sets,
    [{"upstream", [[{connection, "hare"}, {exchange, "upstream"}]] }
    ]},
   {connections, [{"hare", [{host, "localhost"},
                            {port, 5673}]}
                 ]}
  ]}
].
