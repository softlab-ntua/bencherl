
{max_random, 100}.

{duration, infinity}.

{concurrent, 100}.

{report_interval, 60}.

{erlange_nodes, ['nonode@nohost']}.

{operations, [{spawning,100} , {global_register,1}, {global_unregister,1}, {global_whereis,1} ]}.
%{operations, [{spawning,1}]}.

{sleep_time_after_ping, 0}.

{sleep_time_before_ping, 0}.

{node_name, local_node_name}.

{initial_global_size, 0}.

{max_num_operarions, 1000000}.










